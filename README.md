# NasdaQ-AI — نظام اكتشاف الأنماط المركّبة (NAS100)

نظام بحث إحصائي لاكتشاف واختبار متانة الأنماط المركّبة (composite patterns) المرتبطة بربح/خسارة صفقات استراتيجية تداول على `OANDA:NAS100USD`، باستخدام سجل الصفقات وبيانات الشموع بفاصل دقيقة واحدة. المنهجية مبنية على: عدم تسريب معلومات مستقبلية (no look-ahead)، التحقق بمنهجية walk-forward عبر السنوات، اختبار مونت كارلو لكل نمط على حدة، تصحيح المقارنات المتعددة (Benjamini-Hochberg FDR)، الأدلة البايزية، وفحوصات الثبات واعتماد النظام السوقي. التفاصيل والنتائج الكاملة موجودة في التقرير النهائي بالعربية (انظر أدناه).

## المتطلبات والتثبيت

```bash
pip install -r requirements.txt
```

Python 3.9+ مطلوب.

## التشغيل

```bash
python main.py
```

يشغّل هذا خط الأنابيب الكامل من البداية للنهاية (~90 ثانية) وينتج:
- `outputs/reports/final_report_arabic.md` — التقرير النهائي (سردي بالعربية، الكود وأسماء الأعمدة بالإنجليزية).
- `outputs/data/*.csv` — جداول الأنماط المرشَّحة والمُسجَّلة (تُعاد توليدها في كل تشغيل، غير متتبَّعة في git).
- `outputs/figures/*.png` — الرسوم البيانية (تُعاد توليدها في كل تشغيل، غير متتبَّعة في git).

يمكن أيضًا تشغيل أي وحدة منفردة للتحقق منها أو تصحيحها، مثال:

```bash
python -m src.pattern_search
python -m src.walk_forward
```

## هيكل المشروع

| المسار | الوصف |
|---|---|
| `analytics_1.csv` | سجل الصفقات (789 صفقة، 2020–2024) |
| `nasdaq_m1_2020_2024.parquet` | شموع دقيقة واحدة لنفس الأداة والفترة |
| `pattern-discovery-prompt.md` | المواصفة الأصلية للمشروع |
| `main.py` | تنسيق خط الأنابيب الكامل |
| `src/data_loading.py` | تحميل وتوصيف البيانات والتحقق من التزامن الزمني |
| `src/excursion.py` | إعادة بناء مسار MFE/MAE لكل صفقة من الشموع |
| `src/exit_quality.py` | تصنيف جودة الخروج والمبلغ المتروك على الطاولة |
| `src/regime_detection.py` | تصنيف النظام السوقي (اتجاه/تذبذب/تقلب) ونقاط التحوّل |
| `src/feature_engineering.py` | ميزات ما-قبل-الدخول (بدون أي تسريب مستقبلي) |
| `src/pattern_search.py` | البحث في فضاء الأنماط المركّبة |
| `src/walk_forward.py` | التحقق عبر نوافذ زمنية متوسّعة (سنة باختبار في كل مرة) |
| `src/monte_carlo.py` | اختبار متانة كل نمط عبر Bootstrap وإعادة التوزيع العشوائي |
| `src/bayesian_evidence.py` | الأدلة البايزية (Beta-Binomial، Bayes Factor) |
| `src/stability_regime.py` | ثبات الحدود واعتماد النمط على نظام سوقي محدَّد |
| `src/ml_shap.py` | طبقة LightGBM + SHAP تفسيرية فقط، عبر walk-forward |
| `src/shap_hypothesis_test.py` | اختبار موجَّه لميزات SHAP الأكثر ثباتًا كفرضية مُسجَّلة مسبقًا |
| `src/clustering.py` | تجميع الفائزين/الخاسرين وأطول سلسلة خسائر |
| `src/edge_decay.py` | تحليل تراجع الحافة عبر الزمن ونقاط التحوّل |
| `src/scoring.py` | Robustness Score المُجمَّع وترتيب الأنماط |
| `src/visualizations.py` | الرسوم البيانية |
| `src/reporting.py` | توليد التقرير النهائي بالعربية |
| `src/stats_utils.py` | أدوات إحصائية مشتركة (bootstrap، FDR، بايزي) |
| `src/config.py` | المسارات والثوابت والافتراضات الموثَّقة |

## اختبار الوقف المتحرك (Trailing Stop) — تحليل افتراضي منفصل

اختبار "ماذا لو" لاحق، منفصل تمامًا عن خط الأنابيب أعلاه ولا يعدّل أي بيانات أصلية (انظر
`trailing-stop-optimization-prompt.md`): هل يحسّن وقف متحرك (بنسب 1%–20% تخلٍّ عن الربح
المفتوح) جودة الخروج مقارنة بالخروج الفعلي، مع تحقق بمونت كارلو واختبار إحصائي مزدوج وفحص
فرط تخصيص عبر تدريب/تحقق؟

```bash
python -m src.trailing_stop_main
```

ينتج `outputs/reports/trailing_stop_report_arabic.md`، وبيانات `outputs/data/trailing_stop_*.csv`،
ورسومًا `outputs/figures/trailing_stop_*.png`.

| المسار | الوصف |
|---|---|
| `src/trailing_stop_simulation.py` | محاكاة الخروج الافتراضي بوقف متحرك، شمعة بشمعة، بدون تسريب مستقبلي |
| `src/trailing_stop_metrics.py` | جدول شبكة النسب الكامل (PF، عائد متوقع، Sharpe، أقصى تراجع) |
| `src/trailing_stop_monte_carlo.py` | Bootstrap لأفضل 3 إعدادات وفترات ثقة معامل الربح |
| `src/trailing_stop_significance.py` | اختبار t مزدوج و Cohen's d مقابل الأصلي |
| `src/trailing_stop_monthly.py` | مقارنة الربح/الخسارة الشهرية |
| `src/trailing_stop_validation.py` | فحص فرط التخصيص عبر تقسيم تدريب/تحقق زمني |
| `src/trailing_stop_regime.py` | اعتماد أفضل إعداد على نظام السوق (اتجاه/تقلب) |
| `src/trailing_stop_visualizations.py` / `trailing_stop_reporting.py` / `trailing_stop_main.py` | الرسوم، التقرير النهائي، والتنسيق |

## اكتشاف الأنماط الخفية + تحسين الخروج حسب نظام الدخول — تحليل افتراضي منفصل

اختبار "ماذا لو" ثانٍ، منفصل تمامًا وغير معتمِد على تحليل الوقف المتحرك أعلاه (انظر
`hidden-patterns-exit-optimization-prompt.md`): هل جودة الخروج المثلى تعتمد على سياق الدخول
(نظام السوق، شكل الشموع، إلخ)؟ يكتشف أنماطًا خفية بالتجميع (K-means) على ميزات ما-قبل-الدخول
حصرًا، يختبر 19 استراتيجية خروج لكل نمط (هدف ربح ثابت، وقف متحرك، خروج زمني، جني أرباح جزئي،
وقف متكيّف مع MFE)، ثم يبني استراتيجية هجينة حسب النمط ويقارنها بأفضل استراتيجية عامة واحدة،
مع تحقق بمونت كارلو وتدريب/تحقق زمني معزول.

```bash
python -m src.hidden_pattern_main
```

ينتج `outputs/reports/hidden_pattern_report_arabic.md`، وبيانات `outputs/data/hidden_pattern_*.csv`،
ورسومًا `outputs/figures/hidden_pattern_*.png`.

| المسار | الوصف |
|---|---|
| `src/hidden_pattern_features.py` | ميزات سياق الدخول (تجميع) + سرعة الربح بعد الدخول (وصفي فقط) |
| `src/hidden_pattern_clustering.py` | تجميع K-means قابل لإعادة الاستخدام على بيانات تدريب/تحقق |
| `src/exit_strategy_simulation.py` | محاكاة موحَّدة شمعة بشمعة لكل استراتيجيات الخروج (19 استراتيجية) |
| `src/exit_strategy_metrics.py` | جدول أداء كل استراتيجية داخل كل نمط، وأفضل استراتيجية عامة |
| `src/exit_strategy_monte_carlo.py` | Bootstrap لكل نمط + اختبار إجهاد بخلط ترتيب الصفقات |
| `src/hybrid_strategy.py` | الاستراتيجية الهجينة مقابل الأصلي ومقابل أفضل استراتيجية عامة واحدة |
| `src/hidden_pattern_monthly.py` | مقارنة شهرية/سنوية وملف التراجع |
| `src/hidden_pattern_validation.py` | تحقق تدريب/تحقق زمني معزول (بدون تسريب بين الفترتين) |
| `src/hidden_pattern_visualizations.py` / `hidden_pattern_reporting.py` / `hidden_pattern_main.py` | الرسوم، التقرير النهائي، والتنسيق |

## اختبار "بلا وقف خسارة" (No Stop-Loss) — تحليل افتراضي منفصل

سؤال مباشر: لو أُزيل وقف الخسارة (`initalSL`) تمامًا وتُركت الصفقة تتحرك حرة، ماذا كان سيحدث؟
منفصل تمامًا عن التحليلين أعلاه ولا يعدّل أي بيانات أصلية: محاكاة شمعة-بشمعة لكل صفقة بلا وقف حتى
مهلة 30 يومًا، مع تتبّع أسوأ نقطة وصلتها الصفقة (وليس فقط النتيجة النهائية)، اختبار إحصائي مزدوج
(t-test و Wilcoxon)، وتحقق بمونت كارلو على مستوى الحساب ككل وحساسية ترتيب الصفقات.

```bash
python -m src.no_stop_main
```

ينتج `outputs/reports/no_stop_report_arabic.md`، وبيانات `outputs/data/no_stop_*.csv`،
ورسومًا `outputs/figures/no_stop_*.png`.

| المسار | الوصف |
|---|---|
| `src/no_stop_simulation.py` | محاكاة الرحلة بلا وقف خسارة، شمعة بشمعة، بدون تسريب مستقبلي |
| `src/no_stop_metrics.py` | مقارنة مع/بدون وقف: متوسط، وسيط، PF، أقصى تراجع، مخاطر الذيل |
| `src/no_stop_monte_carlo.py` | اختبار مزدوج (t-test + Wilcoxon)، Bootstrap للأثر على الحساب، حساسية ترتيب الصفقات |
| `src/no_stop_visualizations.py` / `no_stop_reporting.py` / `no_stop_main.py` | الرسوم، التقرير النهائي، والتنسيق |

**خلاصة سريعة**: إزالة الوقف كانت لتحسّن نصف الصفقات المتوقفة تقريبًا (كانت لتتعافى وتربح)، لكنها تعرّض
الحساب لخسارة كارثية غير محدودة في عدد قليل من الصفقات (أسوأها -407R) — التفاصيل الكاملة في التقرير.

## اختبار "الهدف أو الوقف بدون تدخّل" (Target-or-Stop) — تحليل افتراضي منفصل

سؤال متابعة مباشر: لو تُركت الصفقة (بنفس الدخول ووقف الخسارة الأصلي) بدون أي خروج يدوي مبكر أو تحريك
وقف، حتى تلمس فعليًا الهدف (`idealTP`) أو الوقف — هل كان الأداء ليختلف عن الخروج الفعلي؟ لا يبني محاكاة
جديدة، بل يُبرز مقارنة محدَّدة من محرك `exit_strategy_simulation.py` الموجود مسبقًا (استراتيجية
`fixed_tp_idealTP` التي تُبقي وقف الخسارة الأصلي فعّالًا طوال الوقت)، مع اختبار إحصائي مزدوج وتحقق
بمونت كارلو مخصَّصين لهذه المقارنة تحديدًا.

```bash
python -m src.target_or_stop_main
```

ينتج `outputs/reports/target_or_stop_report_arabic.md`، وبيانات `outputs/data/target_or_stop_*.csv`،
ورسومًا `outputs/figures/target_or_stop_*.png`.

| المسار | الوصف |
|---|---|
| `src/target_or_stop_significance.py` | اختبار مزدوج (t-test + Wilcoxon) بين الخروج الفعلي و`fixed_tp_idealTP` |
| `src/target_or_stop_monte_carlo.py` | Bootstrap مزدوج لفترة ثقة معامل الربح |
| `src/target_or_stop_visualizations.py` / `target_or_stop_reporting.py` / `target_or_stop_main.py` | الرسوم، التقرير النهائي، والتنسيق |

**خلاصة سريعة**: معامل الربح يتضاعف تقريبًا (1.18 ← 2.42) بدلالة إحصائية قوية جدًا (p≈9×10⁻²⁶) وتأكيد
Monte Carlo بنسبة 100%. **تحفّظ جوهري**: `idealTP` أفضل سعر تحقّق بأثر رجعي وليس هدفًا كان معروفًا مسبقًا
— التفاصيل والتفسير الصحيح في التقرير.

### نسخة تفاعلية (HTML)

بطاقات KPI + جدول شهري وتفصيلي قابلين للفرز والفلترة، بنفس شكل تقرير سابق كان محفوظًا كـ PDF من جلسة
سابقة على هذا المشروع — أُعيد بناؤه هنا من الكود الحالي (تحقّقت الأرقام مطابقة 100% رقميًا)، مع إضافة
تحفّظ مُقاس رقميًا حول حالة نادرة: عندما يقع وقف الخسارة والهدف داخل نفس شمعة الدقيقة الواحدة (9 صفقات
فقط، أثر أقصى -2.4% على الرقم الإجمالي — راجع `src/target_or_stop_data.py::find_same_candle_ties`).

```bash
python -m src.target_or_stop_html_report
```

ينتج `outputs/reports/target_or_stop_interactive_report.html` — يُفتَح مباشرة في أي متصفح، بدون خادم
أو اتصال إنترنت (كل الأنماط والبيانات والجافاسكربت مضمَّنة داخل الملف نفسه).

| المسار | الوصف |
|---|---|
| `src/target_or_stop_data.py` | جدول كل صفقة + الملخص الشهري + كشف حالات تعارض الهدف/الوقف بنفس الشمعة |
| `src/target_or_stop_html_report.py` | يبني ملف HTML التفاعلي المستقل (بدون أي اعتماديات خارجية) |

### نسخة "بدون تبييت" (إغلاق إجباري 16:00 نيويورك)

قاعدة محدَّدة من المستخدم: نفس "الهدف أو الوقف"، لكن مع إغلاق إجباري بسعر السوق الساعة 16:00 بتوقيت
نيويورك من نفس يوم الدخول إذا لم يتحقق الهدف أو الوقف بعد — بدون أي تبييت لليوم التالي مطلقًا. **النتيجة
هي الأقوى في كل المشروع**: معامل ربح 3.09 (مقابل 1.18 فعليًا)، أقصى تراجع 16.1R فقط (أقل حتى من نسخة
بلا حد زمني)، دلالة إحصائية قوية جدًا (p≈4.5×10⁻²⁰).

```bash
python -m src.eod_target_or_stop_main
```

ينتج `outputs/reports/eod_target_or_stop_report_arabic.md` ورسم `outputs/figures/eod_target_or_stop_equity.png`.

| المسار | الوصف |
|---|---|
| `src/eod_target_or_stop_simulation.py` | محاكاة الهدف/الوقف مع إغلاق إجباري بموعد قابل للتعديل |
| `src/eod_target_or_stop_reporting.py` / `eod_target_or_stop_main.py` | التقرير والتنسيق |

### أداة قرار لصفقة واحدة (بدون تنفيذ فعلي)

تتحقّق من صفقة جديدة مقابل النمط الوحيد المؤكَّد (تجنّب)، وتطبّق قاعدة "الهدف أو الوقف" ميكانيكيًا على
صفقة تتابعها — لا تفتح ولا تغلق أي صفقة حقيقية أبدًا، فقط تقرأ بيانات الشموع المُعطاة لها.

```bash
python -m src.trade_decision_helper check-entry --datetime "2025-01-15 09:45:00"
python -m src.trade_decision_helper track --entry 21500 --sl 21480 --target 21550 --side buy --datetime "2025-01-15 09:45:00"
```

## ارتباطات خارجية (ذهب/دولار/يورو/VIX/S&P 500) — تحليل افتراضي منفصل

سؤال مباشر: هل يبحث المشروع عن أنماط مركّبة مع أصول خارجية عن NAS100؟ الخط الأساسي مبني عمدًا من بيانات
NAS100 الداخلية فقط (حسب الطلب الأصلي)، فهذا تحليل منفصل تمامًا يضيف 5 مؤشرات خارجية (VIX، مؤشر دولار
تقريبي، يورو/دولار، S&P 500 كبديل لأرباح الشركات، والذهب) من مصادر رسمية مجانية (FRED) عدا الذهب
(Yahoo Finance، غير رسمي — أفضل بديل متاح لأن سلسلة LBMA الرسمية بـFRED متوقفة منذ 2015)، بنفس منهجية
عدم تسريب المعلومات المستقبلية، وبنفس محرك البحث والتحقق الإحصائي الكامل (walk-forward + مونت كارلو +
تصحيح Bonferroni + أدلة بايزية).

```bash
python -m src.macro_correlation_main
```

ينتج `outputs/reports/macro_correlation_report_arabic.md`، وبيانات خام مخزَّنة في `external_data/`
(مُتتبَّعة في git للاستنساخ بدون إنترنت لاحقًا) وبيانات النتائج في `outputs/data/macro_correlation_*.csv`.

| المسار | الوصف |
|---|---|
| `src/external_data.py` | جلب وتخزين مؤشرات خارجية من FRED (رسمي) وYahoo Finance (غير رسمي، الذهب فقط) |
| `src/macro_feature_engineering.py` | ميزات ما-قبل-الدخول من المؤشرات الخارجية (بقيمة اليوم السابق فقط) |
| `src/macro_pattern_search.py` / `macro_correlation_reporting.py` / `macro_correlation_main.py` | البحث، التقرير، والتنسيق |

**خلاصة سريعة**: لا يوجد نمط مركّب ناجٍ من أصل 489 توليفة (0 نجت من تصحيح Bonferroni) — رغم أن الارتباط
العام على مستوى المؤشر منطقي ومتوقَّع (NAS100 مقابل VIX: -0.62، مقابل S&P 500: +0.80). نتيجة سلبية صادقة،
وليست فشلًا بالأداة.

## ملاحظات

- `nasdaq_m1_2020_2024.parquet` بحجم ~51MB مخزَّن مباشرة في git (وليس عبر Git LFS — تم تجربة ذلك ثم التراجع عنه بسبب تجاوز حصة LFS المجانية على GitHub).
- جميع الأرقام في `outputs/reports/final_report_arabic.md` مُشتقَّة مباشرة من تشغيل فعلي لخط الأنابيب على البيانات المرفقة، وليست تقديرية.
- مجلد `external_data/` يحتوي بيانات خارجية مجلوبة من مصادر ثالثة (FRED، Yahoo Finance) — راجع
  `outputs/reports/macro_correlation_report_arabic.md` للتفاصيل الكاملة عن كل مصدر وحدود موثوقيته.
